import React from 'react'

// Placeholder: In case we later need React context around Zustand or other stores
export function StoreProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

